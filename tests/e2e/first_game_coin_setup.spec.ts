import { test } from '@playwright/test';

test.describe('E2E: first game coin setup', () => {
  test.skip('game 1 setup initializes coin values and affects first valid trade outcome', async () => {
    // Pending first-game coin initialization contract in authoritative state.
  });
});
