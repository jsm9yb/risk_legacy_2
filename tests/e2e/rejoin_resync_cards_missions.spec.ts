import { test } from '@playwright/test';

test.describe('E2E: rejoin and resync for cards/missions', () => {
  test.skip('drop/rejoin during card/mission transitions converges without duplicate rewards', async () => {
    // Pending mission/card transition events and resync semantics.
  });
});
