import { describe, it } from 'vitest';

describe('Setup flow component integration', () => {
  it.todo('reflects first-game coin setup effects from authoritative state where surfaced');
  it.todo('renders setup-phase reject/resync messages from authoritative payloads');
});
